<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.heima.wemedia.mapper.WmNewsMapper">

    <resultMap id="BaseResultMap" type="com.heima.model.wemedia.pojos.WmNews">
        <id column="id" property="id"/>
        <result column="user_id" property="userId"/>
        <result column="title" property="title"/>
        <result column="content" property="content"/>
        <result column="type" property="type"/>
        <result column="channel_id" property="channelId"/>
        <result column="labels" property="labels"/>
        <result column="created_time" property="createdTime"/>
        <result column="submited_time" property="submitedTime"/>
        <result column="status" property="status"/>
        <result column="publish_time" property="publishTime"/>
        <result column="reason" property="reason"/>
        <result column="article_id" property="articleId"/>
        <result column="images" property="images"/>
        <result column="enable" property="enable"/>
    </resultMap>

    <sql id="Base_Column_List">
        id, user_id, title, content, type, channel_id, labels,
        created_time, submited_time, status, publish_time, reason,
        article_id, images, enable
    </sql>

    <!-- 根据用户ID查询文章列表 -->
    <select id="findByUserId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from wm_news
        where user_id = #{userId}
        order by created_time desc
    </select>

    <!-- 根据状态查询文章列表 -->
    <select id="findByStatus" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from wm_news
        where status = #{status}
        order by created_time desc
    </select>

    <!-- 根据用户ID和状态查询文章列表 -->
    <select id="findByUserIdAndStatus" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from wm_news
        where user_id = #{userId}
        and status = #{status}
        order by created_time desc
    </select>

    <!-- 根据文章ID和用户ID查询文章 -->
    <select id="findByIdAndUserId" resultMap="BaseResultMap">
        select
        <include refid="Base_Column_List"/>
        from wm_news
        where id = #{id}
        and user_id = #{userId}
    </select>

    <!-- 统计用户文章数量 -->
    <select id="countByUserId" resultType="int">
        select count(*)
        from wm_news
        where user_id = #{userId}
    </select>

    <!-- 统计指定状态的文章数量 -->
    <select id="countByStatus" resultType="int">
        select count(*)
        from wm_news
        where status = #{status}
    </select>

</mapper>
